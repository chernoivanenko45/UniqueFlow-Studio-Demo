{
  "name": "uniqueflow-beta-delivery",
  "private": true,
  "type": "module",
  "version": "0.9.0-rc1",
  "scripts": {
    "test": "node --test",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.30.0"
  }
}
